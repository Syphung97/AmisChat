import { GridFieldConfig } from '../grid-field-config/grid-field-config';
import { GridSelectionType } from '../../enum/common/grid-selection-type.enum';

export class GridConfig {
    SelectGridType: GridSelectionType;
    ColumnConfig: any;
}